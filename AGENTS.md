# WeekPilot Agent Guide

## 项目目标

WeekPilot 是一个本地运行的周报生成与工作复盘工具。它读取本周工作日志、任务表和会议纪要，通过大模型生成结构化周报，并保存历史周报。

## 开发原则

- MVP 先只做周报，不做月报。
- 优先保证可运行、结构清晰、方便演示。
- 正式周报生成依赖大模型能力；无 API Key 时只能使用明确标记的 demo 模式。
- 本地程序负责采集、结构化、脱敏、prompt 构建、历史管理、质量检查和导出。
- API Key 不得写死在代码里，不得提交真实密钥。
- 样例数据使用通用职场场景。
- 代码要清晰、可测试，不引入不必要的重型依赖。
- 修改后运行相关测试，至少覆盖 extractor、summarizer、carryover。

## 前端方向

- Streamlit 是本地工作台，不做营销页。
- 页面优先级：本周工作台、历史周报、设置。
- 首页必须清楚展示本周时间范围、输入状态、模型状态和生成入口。
- 历史周报需要可查看，并按周展示。
- 设置页维护 API Key、模型设置、周期设置、报告风格和隐私脱敏选项。
